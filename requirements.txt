# torch için CPU-only wheel index'i (GPU yok, ~2 GB daha küçük image)
--extra-index-url https://download.pytorch.org/whl/cpu

requests
beautifulsoup4
lxml
transformers
torch
plotly
python-dotenv
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.8.0
